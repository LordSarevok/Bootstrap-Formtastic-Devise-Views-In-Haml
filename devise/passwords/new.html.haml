%h2 ¿Olvidaste tu contraseña?

= semantic_form_for(resource, as: resource_name, url: password_path(resource_name)) do |f|
  = f.semantic_errors
  = f.inputs do
    = f.input :email
  = f.buttons do
    = f.commit_button button_html: {class: "primary", value: "Enviar las instrucciones de cambio de contraseña"}

= render "devise/shared/links"
